<?xml version="1.0"?>
<ruleset name="QS-Tool PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        PMD ruleset for QS-Tool project
    </description>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <!-- Allow JUnit 5 assertion usage -->
        <exclude name="JUnitTestsShouldIncludeAssert"/>
        <!-- Allow flexible test method names -->
        <exclude name="JUnitTestContainsTooManyAsserts"/>
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <!-- Allow short variable names in loops -->
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
        <!-- Allow at least one constructor -->
        <exclude name="AtLeastOneConstructor"/>
        <!-- More flexible comment requirements -->
        <exclude name="CommentDefaultAccessModifier"/>
        <exclude name="OnlyOneReturn"/>
        <exclude name="LocalVariableCouldBeFinal"/>
        <exclude name="MethodArgumentCouldBeFinal"/>
    </rule>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <!-- Flexible complexity limits -->
        <exclude name="TooManyMethods"/>
        <exclude name="LawOfDemeter"/>
        <exclude name="LoosePackageCoupling"/>
        <exclude name="DataClass"/>
    </rule>

    <!-- Adjust CyclomaticComplexity threshold -->
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="classReportLevel" value="80"/>
            <property name="methodReportLevel" value="10"/>
        </properties>
    </rule>

    <!-- Documentation -->
    <rule ref="category/java/documentation.xml">
        <!-- Flexible Javadoc requirements -->
        <exclude name="CommentRequired"/>
        <exclude name="CommentSize"/>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <!-- Allow bean member usage -->
        <exclude name="BeanMembersShouldSerialize"/>
        <!-- Allow null assignments -->
        <exclude name="NullAssignment"/>
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml"/>

    <!-- Performance -->
    <rule ref="category/java/performance.xml"/>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>

</ruleset>

